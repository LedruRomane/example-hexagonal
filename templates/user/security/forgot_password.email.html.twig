<h1>Bonjour</h1>

<p>Pour réinitialiser votre mot de passe, veuillez utiliser ce lien :</p>

<a href="{{ front_url('front_reset_password', { token: resetToken.token }) }}">
    {{ front_url('front_reset_password', { token: resetToken.token }) }}
</a>

<p>
    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(
        resetToken.expirationMessageData,
        'ResetPasswordBundle',
    ) }}.
</p>

<p>À bientôt !</p>
